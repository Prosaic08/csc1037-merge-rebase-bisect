*After* merging the "subtract" and "multiply" branches into "master",
rebase this branch onto "master".

Do not merge this branch.

Rather, by rebasing, you are re-writing this branch such that, were it
merged into master, it would be merged by fast forwarding.
